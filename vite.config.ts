import { defineConfig } from "vite";

// Publicando en usuario.github.io/mas-inmuebles (sin dominio propio todavía).
// Cuando conectes un dominio propio, cambiá esto de nuevo a base: "/".
export default defineConfig({
  base: "/mas-inmuebles/",
});
